import React from 'react';
import injectSheet from 'react-jss';
import propTypes from 'prop-types';


import TextInput from '../atoms/TextInput';

const Form = ({ classes, fields, handleSubmit}) => {
	var arr = fields.map( field => {
		
	})

	return (
		)
}